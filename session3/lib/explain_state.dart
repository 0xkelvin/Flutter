import 'package:flutter/material.dart';

class ExplainState extends StatelessWidget {
  const ExplainState({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Text('sleepy'),
    );
  }
}